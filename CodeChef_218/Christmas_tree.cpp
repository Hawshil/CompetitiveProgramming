#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // int t;
    // cin >> t;
    // while (t--)
    // {
    //     int n
    // }

    int n, a, m, b;

    cin >> n >> a >> m >> b;

    cout << (b*m) - (a*n) << endl;
    return 0;
}
